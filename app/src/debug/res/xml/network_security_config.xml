<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuração de rede do build de **depuração**.

  Mesma regra do release — texto claro proibido —, com uma exceção nominal: o backend rodando na
  máquina do desenvolvedor, alcançado do emulador por `10.0.2.2` ou de um dispositivo por
  `localhost`. A exceção é por host, e não um relaxamento global, e não é compilada no APK de
  release: este arquivo só existe no source set `debug`.

  O endereço de desenvolvimento vem de `-PsparkBackendBaseUrlDebug=...`; ele nunca é default e
  nunca vale para release.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
