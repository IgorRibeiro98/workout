package com.example.domain.evolution.model

enum class EvolutionPeriod(val label: String) {
    ALL_TIME("Todo o período"),
    LAST_30_DAYS("Últimos 30 dias"),
    LAST_90_DAYS("Últimos 90 dias"),
    CURRENT_YEAR("Ano atual")
}
