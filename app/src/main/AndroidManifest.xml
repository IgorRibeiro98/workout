<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
        Alarme exato do fim de descanso (auditoria 2026-09-12).

        `SCHEDULE_EXACT_ALARM` fica: é a permissão concedível pelo usuário para um alarme que ele
        mesmo marcou, e é ela que o `WorkoutNotificationManager` checa antes de agendar exato. A
        partir do Android 14 ela nasce negada para quem mira 33+; a tela de Configurações oferece o
        atalho para o usuário concedê-la, e até lá o alarme é inexato.

        Duas permissões saíram daqui, sem perda de função:

        - `USE_EXACT_ALARM`: o Play a restringe a apps cuja função central é despertador,
          temporizador ou calendário. Declará-la num app de treino é risco de recusa na revisão, e
          ela só servia para pular a concessão pelo usuário.
        - `FOREGROUND_SERVICE`: o app não tem **nenhum** serviço em primeiro plano (zero ocorrências
          de `startForeground`). Só aparecia na ficha do Play como um poder que o Spark não usa.
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    
        <receiver
            android:name=".service.RestNotificationReceiver"
            android:exported="false" />

        <!-- Serviço FCM de notificações sociais (T17.5) -->
        <service
            android:name=".service.SparkFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
            Inicialização sob demanda do WorkManager (T16.6).

            O inicializador padrão é um `ContentProvider` que roda no **startup** de todo app que
            tenha WorkManager no classpath. O Spark é local-first: quem nunca ativou a nuvem não
            deve pagar nada por um agendador que ele não usa. Removendo este nó e implementando
            `Configuration.Provider` em `MainApplication`, o WorkManager só é construído na
            primeira vez que alguém chama `WorkManager.getInstance(...)` — ou seja, quando uma
            alteração local precisa ser agendada.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
